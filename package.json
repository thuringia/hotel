{
  "name": "@thuringia/hotel",
  "private": true,
  "devDependencies": {
    "greenkeeper-lockfile": "^1.15.1",
    "lerna": "^3.10.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "pretest": "eslint packages",
    "test": "jest",
    "_:prettier": "prettier --write",
    "fmt:js": "yarn _:prettier \"**/*.{js,jsx}\""
  }
}
