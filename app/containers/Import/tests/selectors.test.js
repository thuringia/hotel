// import { fromJS } from "immutable";
// import { makeSelectImportDomain } from "../selectors";

// const selector = makeSelectImportDomain();

describe("makeSelectImportDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
