import { throttle, put } from "redux-saga/effects";
import { actionCreators, DEFAULT_ACTION } from "./duck";

function* defaultSaga(action) {
  yield put(actionCreators.defaultAction({}, action.id));
}

function* bootstrap() {
  yield throttle(300, DEFAULT_ACTION, defaultSaga);
}

export default [bootstrap];
