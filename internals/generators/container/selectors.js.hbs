import { createSelector } from "reselect";

const select{{ properCase name }}Domain = id => state => state.get("{{ properCase name }}").get(id);

const makeSelect{{ properCase name }} = id =>
  createSelector(
    select{{ properCase name }}Domain(id),
    substate => (substate ? substate.toJS() : {})
  );

export default makeSelect{{ properCase name }};

export { select{{ properCase name }}Domain };
